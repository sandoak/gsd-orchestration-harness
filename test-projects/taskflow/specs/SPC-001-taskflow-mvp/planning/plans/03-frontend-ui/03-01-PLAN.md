---
phase: 03-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: ['01-03']
files_modified:
  - packages/web/src/components/TaskItem.tsx
  - packages/web/src/components/TaskItem.css
  - packages/web/src/components/TaskList.tsx
  - packages/web/src/components/TaskList.css
  - packages/web/src/components/index.ts
autonomous: true
must_haves:
  truths:
    - 'TaskItem renders task with checkbox and delete button'
    - 'TaskList displays tasks grouped by completion status'
    - 'Loading and error states are handled'
  artifacts:
    - path: 'packages/web/src/components/TaskItem.tsx'
      provides: 'Individual task display'
    - path: 'packages/web/src/components/TaskList.tsx'
      provides: 'Task list with grouping'
      exports: ['TaskList']
  key_links: []
must_pass:
  - id: file-exists-task-item
    type: file_exists
    path: packages/web/src/components/TaskItem.tsx
  - id: file-exists-task-list
    type: file_exists
    path: packages/web/src/components/TaskList.tsx
---

<objective>
Create React components to display a list of tasks with completion toggle and delete functionality.

Purpose: Provide UI for viewing and managing tasks.
Output: TaskList and TaskItem components ready for API integration.
</objective>

<execution_context>
@.harness/skills/workflows/execute-plan.md
@.harness/skills/templates/summary.md
</execution_context>

<context>
@specs/SPC-001-taskflow-mvp/SPEC.md
@specs/SPC-001-taskflow-mvp/execution/phases/01-foundation/01-03-SUMMARY.md
@packages/shared/src/types.ts

**Component structure:**

- TaskItem: Individual task with checkbox, title, description, delete button
- TaskList: List of TaskItem components, grouped by completion status
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskItem component</name>
  <files>packages/web/src/components/TaskItem.tsx, packages/web/src/components/TaskItem.css</files>
  <action>
    1. Create packages/web/src/components/TaskItem.tsx:
       ```typescript
       import type { Task } from '@taskflow/shared';
       import './TaskItem.css';

       interface TaskItemProps {
         task: Task;
         onToggle: (id: string, completed: boolean) => void;
         onDelete: (id: string) => void;
       }

       export function TaskItem({ task, onToggle, onDelete }: TaskItemProps) {
         return (
           <div className={`task-item ${task.completed ? 'completed' : ''}`} data-testid="task-item">
             <div className="task-checkbox">
               <input
                 type="checkbox"
                 checked={task.completed}
                 onChange={() => onToggle(task.id, !task.completed)}
                 aria-label={`Mark "${task.title}" as ${task.completed ? 'incomplete' : 'complete'}`}
               />
             </div>
             <div className="task-content">
               <h3 className="task-title">{task.title}</h3>
               {task.description && <p className="task-description">{task.description}</p>}
               <span className="task-date">Created: {new Date(task.createdAt).toLocaleDateString()}</span>
             </div>
             <button className="task-delete" onClick={() => onDelete(task.id)} aria-label={`Delete "${task.title}"`}>
               Delete
             </button>
           </div>
         );
       }
       ```

    2. Create packages/web/src/components/TaskItem.css with styling:
       - Flexbox layout with checkbox, content, delete button
       - Completed state with opacity and strikethrough
       - Hover effects and transitions

  </action>
  <verify>
    - ls packages/web/src/components/TaskItem.tsx packages/web/src/components/TaskItem.css
  </verify>
  <done>
    - TaskItem component created
    - Checkbox, title, description, delete button
    - Accessible with aria-labels
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TaskList component</name>
  <files>packages/web/src/components/TaskList.tsx, packages/web/src/components/TaskList.css, packages/web/src/components/index.ts</files>
  <action>
    1. Create packages/web/src/components/TaskList.tsx:
       ```typescript
       import type { Task } from '@taskflow/shared';
       import { TaskItem } from './TaskItem';
       import './TaskList.css';

       interface TaskListProps {
         tasks: Task[];
         loading?: boolean;
         error?: string | null;
         onToggle: (id: string, completed: boolean) => void;
         onDelete: (id: string) => void;
       }

       export function TaskList({ tasks, loading, error, onToggle, onDelete }: TaskListProps) {
         if (loading) {
           return <div className="task-list-loading" data-testid="task-list-loading">Loading tasks...</div>;
         }

         if (error) {
           return <div className="task-list-error" data-testid="task-list-error">Error: {error}</div>;
         }

         if (tasks.length === 0) {
           return <div className="task-list-empty" data-testid="task-list-empty">No tasks yet. Create one!</div>;
         }

         const incompleteTasks = tasks.filter(t => !t.completed);
         const completedTasks = tasks.filter(t => t.completed);

         return (
           <div className="task-list" data-testid="task-list">
             {incompleteTasks.length > 0 && (
               <section className="task-section">
                 <h2>To Do ({incompleteTasks.length})</h2>
                 <div className="task-items">
                   {incompleteTasks.map(task => (
                     <TaskItem key={task.id} task={task} onToggle={onToggle} onDelete={onDelete} />
                   ))}
                 </div>
               </section>
             )}
             {completedTasks.length > 0 && (
               <section className="task-section completed-section">
                 <h2>Completed ({completedTasks.length})</h2>
                 <div className="task-items">
                   {completedTasks.map(task => (
                     <TaskItem key={task.id} task={task} onToggle={onToggle} onDelete={onDelete} />
                   ))}
                 </div>
               </section>
             )}
           </div>
         );
       }
       ```

    2. Create TaskList.css with styling

    3. Create packages/web/src/components/index.ts:
       ```typescript
       export { TaskList } from './TaskList';
       export { TaskItem } from './TaskItem';
       ```

  </action>
  <verify>
    - ls packages/web/src/components/TaskList.tsx packages/web/src/components/index.ts
  </verify>
  <done>
    - TaskList component created
    - Tasks grouped by completion status
    - Loading, error, empty states handled
    - Components exported from index
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TaskItem.tsx exists with checkbox, delete button
- [ ] TaskList.tsx exists with grouping logic
- [ ] index.ts exports both components
- [ ] Components compile without TypeScript errors
</verification>

<success_criteria>

- Components render correctly
- Proper accessibility attributes
- Ready for API integration
  </success_criteria>

<output>
After completion, create `specs/SPC-001-taskflow-mvp/execution/phases/03-frontend-ui/03-01-SUMMARY.md`
</output>
